{
  "name": "fintecpessoal",
  "version": "0.1.0",
  "private": true,
  "description": "FINTECPESSOAL — controle e inteligência financeira pessoal (frontend assets).",
  "scripts": {
    "tailwind:watch": "tailwindcss -i ./static/css/input.css -o ./static/css/tailwind.css --watch",
    "tailwind:build": "tailwindcss -i ./static/css/input.css -o ./static/css/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "tailwindcss": "^4"
  }
}
